<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bjsxt.pojo.People">
    <select id="selAll" resultType="com.bjsxt.pojo.People">
        select * from people
    </select>

    <select id="selOne" resultType="com.bjsxt.pojo.People" parameterType="int">
        select * from people where id =#{0}
    </select>

    <select id="selOneByMap" resultType="com.bjsxt.pojo.People" parameterType="map">
        select * from people where id =#{id} and name =#{name}
    </select>

    <select id="selOneByPeople" resultType="com.bjsxt.pojo.People" parameterType="com.bjsxt.pojo.People">
        select * from people where id =${id} and name ='${name}'
    </select>

    <select id="selPage" resultType="com.bjsxt.pojo.People" parameterType="map">
        select * from people limit #{pageStart},#{pageSize}
    </select>
</mapper>
